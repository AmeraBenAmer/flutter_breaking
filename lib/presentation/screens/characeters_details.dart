import 'package:flutter/cupertino.dart';

class CharacterDetialsScreen extends StatelessWidget {
  const CharacterDetialsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
